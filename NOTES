Run

find . -type f -exec dos2unix {} +

to remove Windows line endings.


Docs:

Ran `sphinx-quickstart`
Add `sys.path.insert(0, os.path.abspath('../toon'))` to conf.py
Add 'sphinx.ext.napoleon' to conf.py (under extensions)
Ran `sphinx-apidoc -f -o doc/source toon` in top-level
Ran `make html` in doc/

Upload/release:

1. Commit (don't forget to increment # in setup.py)

(via Travis)
2. `git tag -a <version> -m <message>`
3. `git push github <version>`

Local (from project root):

2. rm dist/*
3. python setup.py bdist_wheel sdist
4. twine upload dist/*
